import { RegisterUntilPipe } from './register-until.pipe';

describe('RegisterUntilPipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterUntilPipe();
    expect(pipe).toBeTruthy();
  });
});
