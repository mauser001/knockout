import { FormatLogPipe } from './format-log.pipe';

describe('FormatLogPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLogPipe();
    expect(pipe).toBeTruthy();
  });
});
