import { TournamentStatePipe } from './tournament-state.pipe';

describe('TournamentStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TournamentStatePipe();
    expect(pipe).toBeTruthy();
  });
});
