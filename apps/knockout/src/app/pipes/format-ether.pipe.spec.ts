import { FormatEtherPipe } from './format-ether.pipe';

describe('FormatEtherPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEtherPipe();
    expect(pipe).toBeTruthy();
  });
});
